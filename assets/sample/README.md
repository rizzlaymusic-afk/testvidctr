Place a small sample video file here for local dev testing.

Recommended file name: sample.webm

If you cannot add a binary file to the repo, the frontend has a remote fallback URL used by the dev "Load Sample Video (dev)" button.

To add a local sample (small):

1. Put a webm/mp4 under assets/sample/sample.webm
2. Re-run `trunk serve` and click "Load Sample Video (dev)" in the drop-zone.

If you want, I can add a tiny placeholder text file here, but a real video must be added by you (binary).
