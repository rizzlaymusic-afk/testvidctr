# Ghost System Dashboard — UI Specification (Sanitized)

This is a cleaned copy of the UI spec intended as a starting template for new projects. Any references to project-specific "waveform" policies were removed.

## Key points (excerpt)
- Zero npm, zero build step: simple single-file dashboard option
- Local-only server: bind to localhost only
- API endpoints: `/api/status`, `/api/findings`, `/api/blast`, `/api/conflicts`, `/api/coverage`, `/api/baseline`

## Feature group naming (example)
- Group 1 — App shell and startup
- Group 2 — Playback
- Group 3 — Track browsing and selection

[This file is a sanitized base-docs snapshot created 2026-04-16.]
